<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.MySearchView" parent="Widget.AppCompat.Light.AutoCompleteTextView">
        <!-- Primary brand color. -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
    </style>

    <style name="AppsTheme" parent="Theme.AppCompat.Light">
        ...
        <item name="autoCompleteTextViewStyle">@style/Theme.MySearchView</item>
    </style>

</resources>